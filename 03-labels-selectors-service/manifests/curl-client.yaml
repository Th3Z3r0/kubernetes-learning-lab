apiVersion: v1
kind: Pod

metadata:
  name: curl-client
  namespace: myk8s

spec:
  restartPolicy: Never
  containers:
    - name: curl
      image: curlimages/curl:latest
      command:
        - sh
        - -c
        - sleep 3600
